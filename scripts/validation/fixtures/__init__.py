"""RecoverLand validation fixtures (LOCAL ONLY).

This package is gitignored. Fixtures are regenerated on demand from
`create_fixtures.py`. The `paths` submodule exposes canonical absolute
paths for scenarios to consume.
"""
